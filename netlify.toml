# Zoho Prep Kit - Netlify deploy config 
[build] 
  command = "npm run build" 
  publish = "dist" 
 
# SPA fallback: serve index.html for client-side routes and hard refreshes 
[[redirects]] 
  from = "/*" 
  to = "/index.html" 
  status = 200 
